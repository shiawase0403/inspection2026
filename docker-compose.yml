version: '3.8'
services:
  app:
    build: .
    image: dorm-inspection-app:latest
    container_name: dorm-inspection-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/database.sqlite
    volumes:
      - ./data:/app/data
